# ── App layer ──
streamlit==1.58.0
python-dotenv==1.2.2

# ── Database / synthetic data ──
mysql-connector-python==9.7.0
Faker==40.23.0

# ── RAG layer ──
pymupdf==1.27.2.3
sentence-transformers==5.6.0
langchain-huggingface==1.2.2
torch==2.12.1
langchain-experimental==0.4.2
langchain-text-splitters==1.1.2
langchain-core==1.4.7
transformers==5.12.1
chromadb==1.5.9

# ── MCP server (tools) ──
mcp==1.28.0

# ── Agent layer (LangGraph + LLM providers + MCP adapter) ──
langgraph==1.2.6
langchain-mcp-adapters==0.3.0
langchain-groq==1.1.3
langchain-google-genai==4.2.5
pydantic==2.13.4                  # structured outputs (agents/schemas.py); direct dep

# ── Observability + evaluation layer (Phase 5) ──
langsmith==0.8.16                 # tracing + eval; direct dep via observability/ + eval/
openpyxl==3.1.5                   # Excel eval-results workbook (eval/results/*.xlsx)
